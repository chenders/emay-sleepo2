# Gradle properties for emay-sleepo2 Android library

android.useAndroidX=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m

# Publishing — set via environment variables in CI, or in ~/.gradle/gradle.properties locally
# ossrh.username=
# ossrh.password=
# signing.keyId=
# signing.password=
# signing.secretKeyRingFile=
